# 福地系统设计

## 1. 福地实体 (xt_fudi)

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGSERIAL | PK |
| `user_id` | BIGINT | 所属用户 (UNIQUE) |
| `tribulation_stage` | INT | 当前劫数 |
| `last_tribulation_time` | TIMESTAMP | 上次天劫时间 |
| `tribulation_win_streak` | INT | 渡劫连胜 |
| `last_online_time` | TIMESTAMP | 最后在线 |
| `create_time` | TIMESTAMP | 创建时间 |
| `update_time` | TIMESTAMP | 更新时间 |

---

## 2. 地块实体 (xt_fudi_cell)

地块存储在独立表中，每个地块一行记录。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGSERIAL | PK |
| `fudi_id` | BIGINT | 所属福地 |
| `cell_id` | INT | 地块编号（从1开始） |
| `cell_type` | VARCHAR(16) | 地块类型：empty/farm/pen |
| `cell_level` | INT | 地块等级（1-5） |
| `config` | JSONB | 建筑专有属性 |
| `create_time` | TIMESTAMP | 创建时间 |
| `update_time` | TIMESTAMP | 更新时间 |

**唯一约束**：`(fudi_id, cell_id)`

### 2.1 灵田 config 示例

```json
{
  "crop_id": 1,
  "plant_time": "2024-01-01T12:00:00",
  "mature_time": "2024-01-02T12:00:00",
  "harvest_count": 0
}
```

灵田 `FarmConfig` 字段释义：

| 字段 | 说明 |
|---|---|
| `crop_id` | 作物模板ID |
| `plant_time` | 种植时间 |
| `mature_time` | 成熟时间 |
| `harvest_count` | 已收获次数（多季作物） |

### 2.2 兽栏 config 示例

```json
{
  "beast_id": 1,
  "template_id": 5,
  "hatch_time": "2024-01-01T12:00:00",
  "mature_time": "2024-01-02T12:00:00",
  "production_stored": [
    {"template_id": 1, "name": "灵草", "quantity": 5}
  ],
  "last_production_time": "2024-01-02T12:00:00"
}
```

兽栏 `PenConfig` 字段释义：

| 字段 | 说明 |
|---|---|
| `beast_id` | 关联灵兽ID |
| `template_id` | 兽卵模板ID（用于产出配置） |
| `hatch_time` | 孵化开始时间 |
| `mature_time` | 孵化完成时间 |
| `production_stored` | 累积产出列表 JSONB |
| `last_production_time` | 上次产出结算时间 |

---

## 3. 地灵实体 (xt_spirit)

地灵与福地 1:1 绑定，存储在独立的 `xt_spirit` 表中。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGSERIAL | PK |
| `fudi_id` | BIGINT | 所属福地 (UNIQUE) |
| `form_id` | INT | 形态ID (FK → xt_spirit_form) |
| `mbti_type` | VARCHAR(4) | MBTI人格 |
| `energy` | INT | 地灵精力 |
| `affection` | INT | 好感度 |
| `affection_max` | INT | 好感度上限 (默认1000) |
| `emotion_state` | VARCHAR(20) | 情绪状态 |
| `last_energy_update` | TIMESTAMP | 上次精力更新时间 |
| `last_gift_time` | TIMESTAMP | 上次送礼时间 |
| `last_event_time` | TIMESTAMP | 上次事件时间 |
| `create_time` | TIMESTAMP | 创建时间 |
| `update_time` | TIMESTAMP | 更新时间 |

---

## 4. 灵石消耗（福地通用）

| 操作 | 灵石消耗 |
|---|---|
| 种植 | `tier × 5` |
| 地块升级 Lv1→Lv2 | 200 |
| 地块升级 Lv2→Lv3 | 400 |
| 地块升级 Lv3→Lv4 | 800 |
| 地块升级 Lv4→Lv5 | 1600 |

灵兽相关操作消耗详见 [灵兽.md](灵兽.md)。

---

## 5. 地块类型 (CellType)

| 类型 | 作用 |
|---|---|
| EMPTY (空地) | 未建造的空地块 |
| FARM (灵田) | 种植灵药 |
| PEN (兽栏) | 孵化/饲养灵兽，详见 [灵兽.md](灵兽.md) |

---

## 6. 地块等级

灵田和兽栏均有独立等级 (Lv1~5)：

| 等级 | 升级消耗 | 累积 | 加速系数 |
|---|---|---|---|
| Lv1 | — | 0 | 1.00× |
| Lv2 | 200 | 200 | 1.15× |
| Lv3 | 400 | 600 | 1.30× |
| Lv4 | 800 | 1400 | 1.45× |
| Lv5 | 1600 | 3000 | 1.60× |

加速系数公式：`加速系数 = 1 + (地块等级 - 最低需求等级) × 0.15`，表格为预计算结果。

---

## 7. 地块扩建

- 初始地块数：**3**（cell_id = 1, 2, 3）
- 增长公式：`maxCells = 3 + floor(tribulationStage / 3)`
- 扩建方式：**自动解锁**，无消耗
- 触发时机：任何状态查询操作时懒加载

---

## 8. 种植系统 (灵田)

**定位**：低门槛、快周转、活跃型。

### 8.1 核心公式

```
种植消耗 = 种子 × 1 + tier × 5 灵石
生长时间 = 基础生长时间 / 等级加速系数
产量 = (1~3) + 劫数/5
收获消耗 = 地灵精力 5 点
```

### 8.2 多季作物

高 tier 种子产出 `1 + tier/3` 次，地块不消失，重置生长进度。

### 8.3 枯萎机制

普通作物：成熟后超 `2×基础生长时间` 未收 → 枯萎消失。
**多季作物免除枯萎检查。**

---

## 9. 天劫系统

天劫系统由独立的 TribulationService 处理，通过 CombatService 构建玩家队伍并调用战斗引擎。天劫 Boss 属性基于防守方队伍总战力动态缩放。

### 9.1 触发机制

- **主动渡劫**：玩家可随时发起渡劫
- **强制渡劫**：距上次劫后 7 天自动触发（状态查询时检查）

**主动渡劫的优势：**
- 可以提前准备（灵兽出战、提升装备等）
- 避免在不利时机被强制渡劫

### 9.2 战斗机制

天劫化身（`TribulationBoss`）接入通用战斗引擎进行回合制战斗，最大 40 回合。

**Boss 属性缩放** — 基于防守方队伍总战力动态缩放：

```
BossHP  = 防守方总HP × (1.0 + 劫数 × 0.5)
Boss攻  = 防守方均攻 × (0.6 + 劫数 × 0.15)
Boss防  = 防守方均防 × (0.4 + 劫数 × 0.10)
Boss速  = 防守方均速 × (0.7 + 劫数 × 0.08)
```

防守方队伍 = 玩家 + 所有出战灵兽（由 `CombatService.buildPlayerTeam()` 构建）。

**怜悯模式**：地灵好感度 ≥ 800 且队伍有存活单位时触发，地灵代主挡劫。

### 9.3 战斗结果

| 结果 | 条件 | 效果 |
|------|------|------|
| **胜利** | 防守方击杀 Boss | 劫数+1、连胜+1、好感+5、灵石奖励（连胜×100）、情绪→EXCITED |
| **怜悯庇护** | 防守方败 + 好感≥800 + 有存活单位 | 劫数+1、连胜+1、灵石奖励、精力归零、情绪→FATIGUED |
| **失败** | 防守方败 + 好感<800 | 劫数/连胜不变、地块摧毁、好感下降、情绪→ANGRY |

### 9.4 地块摧毁规则

摧毁数量与战斗表现挂钩（Boss 剩余 HP 越少，输得越体面）：

| Boss 剩余HP | 摧毁数量 |
|-------------|---------|
| ≥ 50% | 60% 已占用地块 |
| 20% ~ 50% | 30% 已占用地块 |
| < 20% | 1 块 |

---

## 10. 指令汇总

### 福地通用

| 指令 | 说明 |
|---|---|
| `福地` | 查看整体状态 |
| `福地地块` | 地块布局详情（显示地块编号） |
| `福地种植 [编号] [种子]` | 种植灵药 |
| `福地收取 [编号]` | 收取灵药/灵兽产出 |
| `福地建造 [编号] [灵田/兽栏]` | 建造地块 |
| `福地拆除 [编号]` | 拆除地块 |
| `福地升级 [编号]` | 升级地块等级 |
| `福地孵化 [编号] [兽卵]` | 孵化灵兽 |
| `福地放生 [编号]` | 放生灵兽 |
| `福地进化 [编号] [升阶/升品]` | 灵兽进化 |
| `灵兽出战 [编号]` | 灵兽出战随行 |
| `灵兽召回 [编号/all]` | 召回出战灵兽 |
| `灵兽恢复 [编号/all]` | 消耗灵石恢复 HP |
| `地灵 [内容]` | 与地灵自然语言交互 |
| `地灵送礼 [物品]` | 赠送礼物给地灵 |

灵兽系统详细设计详见 [灵兽.md](灵兽.md)。
