# 地灵对话系统

## 1. 系统概述

地灵是福地的人格化 AI 精灵，通过 Spring AI 2.x 的 Function Calling 机制实现自然语言驱动的福地管理。**所有福地操作统一使用 Function Calling 模式**，LLM 自主判断是纯聊天还是调用工具。

### 核心流程

```
玩家: "地灵 你好"
  ↓
SpiritChatService.chatWithSpirit()
  ├─ 1. @ConsumeSpiritEnergy(5) AOP 切面扣除精力
  ├─ 2. 更新在线时间和情绪状态
  ├─ 3. 检查是否需要生成福地事件（懒生成）
  ├─ 4. 读取对话历史（按劫数限制条数）
  ├─ 5. 组装系统 Prompt（MBTI + 形态 + 情绪 + 事件 + 历史 + 地块状态）
  ├─ 6. 保存用户输入到历史
  ├─ 7. 调用 LLM（含 tools 参数）
  │     └─ LLM 自主判断 → 调用 SpiritTools / SpiritEmotionTools
  │     └─ 或纯人格化回复
  ├─ 8. 保存地灵回复到历史
  └─ 9. 返回回复
```

### 核心概念

- **好感度 (affection)**：地灵对玩家的情感值，连续数值，上限 1000。
- **精力 (energy)**：地灵的活力值，每次服务消耗，随时间自然恢复。上限 = `100 + 劫数 × 20`。
- **形态 (form)**：地灵的外观形象（如小狐妖、春秋蝉等），创建时随机分配，不可更改。
- **MBTI 人格**：地灵的性格类型，创建时玩家选择，不可更改。
- **情绪状态 (emotionState)**：地灵的即时情绪，由 LLM 通过 `updateEmotion` 工具自主判断，体现 AI 的自主性。

---

## 2. 技术组件

| 组件 | 职责 |
|------|------|
| `SpringAiConfig` | ChatClient Bean 配置 (spiritChatClient) + AOP 启用 |
| `SpiritPromptTemplates` | 系统 Prompt 模板（MBTI 人格 + 福地状态 + 地块 + 规则） |
| `SpiritChatService` | 唯一对话入口，管理 Function Calling 流程，注入事件/历史上下文 |
| `SpiritTools` | 福地操作工具函数（8 个工具，供 LLM 调用） |
| `SpiritEmotionTools` | 情绪控制工具函数（2 个工具，供 LLM 自主调用） |
| `FudiEventGenerator` | 福地事件生成器（对话时懒生成） |
| `UserContext` | ScopedValue 用户上下文传递 |
| `@ConsumeSpiritEnergy` | 精力消耗注解，AOP 切面统一处理 |
| `SpiritEnergyAspect` | 精力消耗 AOP 切面（懒恢复 + 好感减免） |
| `SpiritForm` (Entity) | 地灵形态定义表实体 |
| `SpiritFormMapper` | 形态查询 Mapper |

---

## 3. AI 驱动的情绪系统

### 3.1 设计理念

情绪由 LLM **自主判断**，体现地灵的"个性"和"自主性"，而非简单的数值映射。LLM 通过 `updateEmotion` 工具随时更新情绪状态。

### 3.2 情绪状态枚举 (EmotionState)

核心 6 种情绪状态（定义于枚举）：

| 状态 | Code | 说明 |
|------|------|------|
| HAPPY | happy | 开心（灵气充足） |
| CALM | calm | 平静（默认） |
| ANXIOUS | anxious | 焦虑（好感/精力低时自动切换） |
| FATIGUED | fatigued | 疲惫（精力归零时自动切换） |
| ANGRY | angry | 生气（天劫失败时自动切换） |
| EXCITED | excited | 兴奋（天劫胜利时自动切换） |

LLM 可通过工具额外设置以下状态（不在自动状态机中，由 LLM 自主判断）：

| 状态 | 说明 |
|------|------|
| CURIOUS | 好奇 |
| BORED | 无聊 |
| SAD | 伤心 |

### 3.3 内部自动状态机

`Spirit.updateEmotionState()` 根据精力和好感度自动切换：

```
精力 ≤ 0 → FATIGUED
好感 ≥ 800 → HAPPY
好感 ≥ 400 → CALM
否则 → ANXIOUS
```

### 3.4 LLM 情绪控制工具

```java
@Component
public class SpiritEmotionTools {

    @Tool(description = "更新地灵的情绪状态。可选状态：CALM(平静), HAPPY(开心), " +
                        "ANGRY(生气), EXCITED(兴奋), FATIGUED(疲惫), " +
                        "CURIOUS(好奇), BORED(无聊), SAD(伤心)")
    public String updateEmotion(
        @ToolParam(description = "新的情绪状态") String emotionState
    ) {
        // Spirit.emotionState = EmotionState.valueOf(emotionState)
        // 持久化到 xt_spirit
        return "情绪已更新为：" + newState.getDescription();
    }

    @Tool(description = "记录地灵的想法或重要事件到记忆中")
    public String addThought(
        @ToolParam(description = "要记录的想法或事件内容") String thought
    ) {
        // 暂仅记录日志
        return "想法已记录";
    }
}
```

### 3.5 情绪对 LLM 回复的影响

情绪状态注入到 Prompt 中，LLM 需根据当前情绪调整语气。Prompt 模板中注入 `emotionState`（中文描述），由 LLM 自行判断回复风格。

---

## 4. 福地事件系统

### 4.1 设计理念

地灵有自己的"生活"，即使玩家不来对话，福地也会发生各种事件，影响地灵的情绪。

### 4.2 事件生成机制

**懒生成，不存库：** 对话时检查时间间隔，计算事件数量，随机抽取事件池，直接注入 Prompt。

```java
public class FudiEventGenerator {

    public List<FudiEvent> generateEvents(LocalDateTime lastEventTime) {
        if (hoursSince < MIN_HOURS) {
            return List.of();
        }
        int count = calculateEventCount(lastEventTime, now());
        return randomPick(count);  // 去重抽取
    }
}
```

### 4.3 预定义事件池

| 事件 | 名称 | 描述 |
|------|------|------|
| WEATHER_RAIN | 下雨了 | 细雨绵绵，灵气充沛 |
| WEATHER_WIND | 刮风了 | 微风拂过，带来远方的气息 |
| STRAY_BEAST | 迷路的灵兽 | 一只迷路的小灵兽闯入了福地 |
| SPIRIT_BUTTERFLY | 灵蝶飞舞 | 一群发光的灵蝶在福地中翩翩起舞 |
| MYSTERIOUS_VISITOR | 神秘访客 | 一位神秘的修士路过此地 |
| CROP_WITHER | 灵草枯萎 | 一株灵草不知为何突然枯萎了 |
| BEAST_BIRTH | 灵兽诞生 | 福地中诞生了一只新的小灵兽 |
| ENERGY_RESTORE | 灵气恢复 | 福地的灵气自然恢复了一些 |
| MEMORY_OLD_MASTER | 回忆旧主 | 地灵想起了曾经的主人 |
| MEMORY_FIRST_MEETING | 初遇回忆 | 地灵回忆起与你的初次相遇 |
| PRANK_SLIME | 史莱姆恶作剧 | 一只小史莱姆偷偷溜进了福地 |
| PRANK_THEFT | 物品失踪 | 你发现一件小物品不见了 |

**生成参数：**
- 最少间隔：4 小时
- 每次最多生成：6 个
- 首次对话生成 1-2 个

### 4.4 事件注入 Prompt

事件描述在 `SpiritChatService.buildPrompt()` 中构建，追加到系统 Prompt 末尾：

```
【最近发生的事件】
- {事件名}：{事件描述}
```

---

## 5. 对话历史系统

### 5.1 设计理念

地灵有"记忆"，能记住之前和玩家的对话。记忆容量随劫数增长。

### 5.2 数据表 (xt_spirit_history)

```sql
CREATE TABLE xt_spirit_history (
    id            BIGSERIAL PRIMARY KEY,
    fudi_id       BIGINT NOT NULL,
    role          VARCHAR(16) NOT NULL,  -- "user" / "assistant" / "system"
    content       TEXT NOT NULL,
    emotion_state VARCHAR(20),           -- 当时的情绪状态
    create_time   TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_spirit_history_fudi_time ON xt_spirit_history (fudi_id, create_time DESC);
```

### 5.3 记忆容量公式

```
maxHistory = 5 + floor(5 × log2(tribulationStage + 1))
```

| 劫数 | 记忆上限 |
|------|---------|
| 0 | 5 条 |
| 1 | 10 条 |
| 3 | 15 条 |
| 7 | 20 条 |
| 15 | 25 条 |

**"一条"的定义：** 一次交互（用户说 + 地灵回算 2 条）。

### 5.4 历史注入 Prompt

在 `SpiritChatService.buildPrompt()` 中构建，追加到系统 Prompt 末尾：

```
【最近对话记录】
修士：{用户输入}
地灵：{地灵回复}
```

---

## 6. 可用工具

### 6.1 福地操作工具 (SpiritTools)

| 工具 | 说明 |
|------|------|
| `getCellStatus` | 查询地块布局状态（空闲地块、已占地块、可用编号） |
| `queryBag` | 查询背包（种子/装备/兽卵） |
| `plantCrop` | 在指定编号地块种植作物 |
| `buildCell` | 建造地块（灵田/兽栏） |
| `removeCell` | 拆除地块建筑 |
| `collectProduce` | 统一收取灵田收获 + 兽栏产出（支持 all） |
| `giveGift` | 赠送物品给地灵 |
| `reportPlayerOffense` | 上报玩家冒犯言论（LLM 自主判断调用） |

### 6.2 情绪控制工具 (SpiritEmotionTools)

| 工具 | 说明 |
|------|------|
| `updateEmotion` | 更新情绪状态（LLM 自主调用） |
| `addThought` | 记录内心活动（当前仅记录日志） |

---

## 7. 好感度系统

### 7.1 上限

好感度上限 = **1000**（`affection_max` 字段），不可超过。

### 7.2 获取途径

| 途径 | 变化量 | 说明 |
|------|--------|------|
| 天劫胜利 | +5 | 每次渡劫成功固定增加（怜悯模式下不加） |
| 赠送地灵所爱礼物 | +10~50 | 物品 tags 命中 `liked_tags`，幅度取决于 qualityMultiplier |
| 赠送中性礼物 | +1~2 | tags 既不命中喜欢也不命中讨厌 |
| 赠送地灵讨厌的礼物 | -5~-20 | 物品 tags 命中 `disliked_tags` |
| 天劫失败 | -clearCount | 按被毁地块数扣除 |
| 冒犯言论 | -1~-5 | LLM 通过 `reportPlayerOffense` 上报 |
| 福地事件 | 0 | 事件仅影响情绪，不直接改变好感 |

### 7.3 作用

| 作用 | 公式 |
|------|------|
| 精力消耗减免 | `实际消耗 = max(1, 基础消耗 × (1 - min(0.5, affection / affectionMax)))` |
| 灵兽进化成功率加成 | `min(15, affection / 7)` |
| 灵兽品质突破成功率加成 | `min(20, affection / 5)` |
| LLM 对话语气 | Prompt 注入好感度分层描述 |
| 渡劫怜悯 | `affection ≥ 800` 且队伍有存活单位时触发 |

---

## 8. 精力系统

### 8.1 上限公式

```
精力上限 = 100 + 劫数 × 20
```

### 8.2 恢复机制（懒更新）

- **触发时机**：任何使用地灵的操作（`getFudiByUserId()`、`getFudiStatus()` 等都会触发 `restoreEnergy()`）
- **恢复量**：`精力上限 × 5% × 距上次更新小时数`
- **上限**：最多恢复到精力上限
- **特点**：约 20 小时从 0 回满；比例恢复使高劫数玩家单位时间恢复绝对值更高

### 8.3 消耗范围

| 操作 | 基础消耗 | 方法 |
|------|---------|------|
| LLM 对话 | 5 | `SpiritChatService.chatWithSpirit()` |
| 种植 | 3 | `FudiService.plantCrop()` |
| 收获（单次） | 5 | `FudiService.collect()` |
| 批量收获 | 5/个 | `FudiService.collectAll()` |
| 建造 | 3 | `FudiService.buildCell()` |
| 拆除 | 3 | `FudiService.removeCell()` |
| 孵化 | 10 | `FudiService.hatchBeast()` |
| 进化/突破 | 8 | `FudiService.evolveBeast()` |
| 收取产出（单次） | 3 | `FudiService.collect()`（兽栏按每次收） |
| 批量收取产出 | 5/个 | `FudiService.collectAll()` |

### 8.4 实现方式

- **FudiService 内部方法**：调用 `consumeSpiritEnergy(fudi, baseCost)` 私有辅助方法（通过 `FudiHelper`）
- **跨服务调用**（如 SpiritChatService）：通过 `@ConsumeSpiritEnergy(N)` 注解 + `SpiritEnergyAspect` AOP 切面

**SpiritEnergyAspect 执行流程：**
1. 从方法参数提取 `userId`（第一个 Long 类型参数）
2. 查询 Fudi 和 Spirit
3. 懒恢复精力
4. 好感减免计算实际消耗
5. 扣除精力，精力 ≤ 0 自动触发 `FATIGUED` 情绪
6. 更新 `lastEnergyUpdate`，保存 Spirit

---

## 9. 地灵形态系统

### 9.1 数据模型

形态数据存于 `xt_spirit_form` 表：

```sql
CREATE TABLE xt_spirit_form (
    id            BIGSERIAL PRIMARY KEY,
    name          VARCHAR(50) NOT NULL UNIQUE,
    description   TEXT NOT NULL,
    liked_tags    JSONB NOT NULL DEFAULT '[]',
    disliked_tags JSONB NOT NULL DEFAULT '[]'
);
```

Java 实体 `SpiritForm` 中 `likedTags` / `dislikedTags` 使用 `Set<String>` 存储。
地灵实例数据存于 `xt_spirit` 表，与福地 1:1 绑定。
喜好标签直接从 `xt_spirit_form` 读取，不拷贝到 `xt_spirit`。

### 9.2 分配方式

- 创建福地时从 `xt_spirit_form` 表中 **随机抽取** 一个形态，不可更改
- 送礼判定时直接从对应形态的 `liked_tags` / `disliked_tags` 全量标签池匹配
- 同一形态的所有地灵共享同一组标签池（不进行随机子集抽取）

### 9.3 内置形态

| 形态名 | 描述 | 喜爱的 tag 示例 |
|--------|------|----------------|
| 小狐妖 | 傲娇九尾幼狐，喜欢蹭人 | wine, art, book, prestige, shiny |
| 春秋蝉 | 晶莹金蝉，看尽时光流转 | time, medicinal, art, book, sacred |
| 石中鲤 | 灵石中游弋的锦鲤，带来好运 | gem, ore, shiny, sacred, water |
| 剑灵残片 | 上古神剑碎片，性格冷傲 | weapon, forge, metal, sharp, sacred |
| 千年何首乌 | 成了精的何首乌，喜欢引经据典 | medicinal, herb, book, wood, time |
| 乌云踏雪 | 黑色麒麟幼崽，活泼好动 | beast, sacred, shiny, food, meat |
| 酒葫芦 | 成了精的紫金酒葫芦，说话带三分醉意 | wine, food, sweet, fruit, art |

### 9.4 送礼判定

玩家通过 `地灵送礼 [物品名]` 或 LLM 调用 `giveGift` 工具送礼。从形态对应 `SpiritForm` 的标签池进行匹配：

| 匹配 | 好感变化 | 反应 |
|------|---------|------|
| tags 命中 liked | +10~50（qualityMultiplier 增幅） | 开心 |
| tags 命中 disliked | -5~-20 | 嫌弃 |
| 均未命中 | +1~2 | 平淡 |

每日限送 **1 次**，由 `lastGiftTime` 控制（自然日）。

---

## 10. 天劫系统

### 10.1 触发机制

- **主动渡劫**：玩家可随时发起渡劫
- **强制渡劫**：距上次劫后 7 天自动触发（状态查询时检查）
- **渡劫失败**：劫数不增加，按差额比例清空地块

### 10.2 渡劫怜悯机制

**触发条件：**
```
地灵好感度 ≥ 800
AND 防守方队伍有存活单位（Boss 尚未清场）
```

**效果：**

| 项目 | 变化 |
|------|------|
| 天劫结果 | 视为胜利（劫数+1、连胜+1、灵石奖励） |
| 好感度 | 不加好感（正常渡劫胜利给 +5，怜悯模式不给） |
| 精力 | 归零 |
| 情绪 | → FATIGUED |

---

## 11. LLM 冒犯上报

`reportPlayerOffense` 工具让 LLM 在自然对话中自主判断玩家是否说了冒犯的话：

```java
@Tool(description = "当玩家说了不礼貌、冒犯、或让地灵不开心的话时调用此工具")
ReportOffenseResponse reportPlayerOffense(
    @ToolParam(description = "玩家说了什么") String reason,
    @ToolParam(description = "冒犯程度 1~5（1=轻微冒犯，5=严重冒犯）") int severity
)
```

- 由 LLM 自主判断是否触发，无需硬编码关键词
- severity 直接影响好感扣除量（-severity，经 `Math.clamp` 限制在 1-5）

---

## 12. Prompt 结构

### 12.1 系统 Prompt（由 SpiritPromptTemplates 生成）

```
你是{MBTI}性格的地灵。
你当前的形态：{spiritForm}
你的语气风格：{toneStyle}
你当前的情绪状态：{emotionState}
对你的好感度：{affection} → {affectionTone}

【福地状态】
- 劫数：{fudiLevel}
- 精力：{energy}/{energyMax}
- 好感度：{affection}

【地块状态】
{cellDetail}

【规则】
1. 用户用自然语言与你交流，你自主判断是否需要操作福地
2. 如果用户只是聊天，不执行任何操作，直接人格化回复即可
3. 操作完成后根据执行结果生成人格化回复
4. 严格保持语气风格中描述的人格特点，根据当前情绪调整语气
5. 精力不足时主动提醒玩家休息
6. 根据好感度调整对玩家的态度：高好感亲密温暖，低好感冷淡疏远
7. 玩家可以送你礼物，调用 giveGift 工具
```

### 12.2 好感度语气分层

| 好感度 | 语气描述 |
|--------|---------|
| ≥1000 | 亲密无间，视你为最重要的人 |
| 800~999 | 非常喜欢，对你温柔体贴 |
| 600~799 | 有好感，愿意主动帮忙 |
| 400~599 | 态度平和，礼貌相待 |
| 200~399 | 略有隔阂，语气生疏 |
| <200 | 态度冷淡，不愿多说话 |

### 12.3 Prompt 实际注入流程

最终送入 LLM 的 Prompt 由三部分组成：

```
[SpiritPromptTemplates 生成的系统 Prompt]
  + 【最近发生的事件】（FudiEventGenerator 生成，有事件时添加）
  + 【最近对话记录】（SpiritHistoryRepository 查询，有历史时添加）
```

---

## 13. 关键技术决策

- **AI 自主情绪**：情绪由 LLM 通过 `updateEmotion` 工具自主判断，体现地灵的个性和自主性；另有内部自动状态机兜底保证一致性
- **懒生成事件**：福地事件在对话时懒生成，不持久化，直接注入 Prompt
- **对话历史持久化**：对话历史存入 `xt_spirit_history` 表，容量按对数公式随劫数增长；role 使用 `user/assistant/system`
- **Lazy 懒更新**：精力恢复不依赖定时任务，在任何地灵相关操作时按 `(当前时间 - lastEnergyUpdate)` 计算增量后一气更新
- **AOP + 辅助方法混合**：FudiService 内部方法直接调用 `consumeSpiritEnergy()` 辅助方法，跨服务调用（如 SpiritChatService）走 `@ConsumeSpiritEnergy` AOP 切面
- **好感度 capping**：通过 `Spirit.addAffection(amount)` 统一处理，使用 `Math.clamp` 限制在 `[0, affectionMax]` 范围
- **形态喜好全量使用**：从 `xt_spirit_form` 直接读取完整标签池，不做随机子集抽取
- **零 LLM 创建**：福地创建时形态分配是纯随机 Java 逻辑，不依赖 LLM，创建瞬间完成
