# 更新路线图

已实现服务清单：

| 服务                                                          | 职责              |
|-------------------------------------------------------------|-----------------|
| `UserService` / `UserAuthService` / `AuthenticationService` | 用户、跨平台认证        |
| `CultivationService`                                        | 突破、护道           |
| `MapService` / `TravelService` / `TrainingService`          | 地图、旅行、历练        |
| `CombatService` + 战斗引擎组件                                    | 自动战斗、动态遇怪、灵兽 AI |
| `ItemService` / `SkillService`                              | 物品装备、法决         |
| `BountyService`                                             | 悬赏              |
| `FudiService`                                               | 福地、地块、种植、灵兽、天劫  |
| `SpiritChatService`                                         | 地灵对话、LLM 集成     |
| `PillService`                                               | 炼丹、丹方、服用        |
| `BeastCommandHandler`                                       | 灵兽命令格式化         |

---

## 进行中

（暂无）

---

## 待实现

### 丹药系统重构

1. 修复 `User.java` 实体 — 映射 `statStr/con/agi/wis` 字段
2. 新增 `xt_pill_resistance` 表 + `xt_player_buff` 表
3. 拆分 `PillService` → `PillRecipeService` / `PillRefiningService` / `PillConsumptionService`
4. `ItemUseService` 统一物品扣减；四效果完整实现（exp/hp/stat/breakthrough）；等级衰减 + 抗性系统
5. `CombatService` / `CultivationService` 接入 `xt_player_buff`（战斗前叠加增益、突破时累加加成）；定期清理过期 Buff

