# 更新路线图

## 概述

本文档记录设计讨论中确认的待更新内容，按优先级和模块分类。

---

## 1. 用户系统

### 1.1 属性系统调整

| 项目 | 当前状态 | 目标状态 | 状态 |
|------|---------|---------|------|
| 属性基础值 | 固定 5 | `4 + level`（等级1=5，等级50=54） | ⏳ 待实现 |
| 自由加点 | 代码中有 `freeStatPoints` | 移除，属性完全由等级决定 | ⏳ 待实现 |
| 洗点系统 | 代码中有 `resetAllStatPoints()` | 移除 | ⏳ 待实现 |

**代码清理：**
- [ ] 移除 `User.freeStatPoints` 字段
- [ ] 移除 `User.lastResetPointsTime` 字段
- [ ] 移除 `User.allocateStatPoints()` 方法
- [ ] 移除 `User.resetAllStatPoints()` 方法
- [ ] 移除 `User.canResetPoints()` 方法
- [ ] 移除 `CultivationService.allocateAttributePoints()` 方法
- [ ] 移除 `CultivationService.resetStatPoints()` 方法

### 1.2 HP 恢复机制

| 项目 | 当前状态 | 目标状态 | 状态 |
|------|---------|---------|------|
| HP 恢复公式 | `maxHp / 100`（1%） | 每5分钟恢复 1% 最大HP | ✅ 已实现 |

**文档已更新：** `用户.md` §7.2

---

## 2. 法决系统

### 2.1 字段调整

| 项目 | 当前状态 | 目标状态 | 状态 |
|------|---------|---------|------|
| 绑定字段 | `bindingType` + `bindingValue` | 保持 | ✅ 已确认 |
| 智慧要求 | 无 | 添加 `require_wis` 字段 | ⏳ 待实现 |
| 前置法决 | 无 | 添加 `require_skill_id` 字段 | ⏳ 待实现 |
| 标签 | 无 | 添加 `tags` JSONB 字段 | ⏳ 待实现 |
| 威力倍率 | `power_multiplier` | 删除 | ⏳ 待实现 |

**数据库迁移：**
```sql
ALTER TABLE xt_skill ADD COLUMN require_wis INTEGER;
ALTER TABLE xt_skill ADD COLUMN require_skill_id BIGINT;
ALTER TABLE xt_skill ADD COLUMN tags JSONB DEFAULT '[]';
ALTER TABLE xt_skill DROP COLUMN power_multiplier;
```

### 2.2 效果类型

| 项目 | 当前状态 | 目标状态 | 状态 |
|------|---------|---------|------|
| 效果类型数量 | 14 种 | 保持 | ✅ 已确认 |

**现有效果类型：**
- 伤害类：DAMAGE, MULTI_HIT, EXECUTE, LIFESTEAL
- 控制类：ARMOR_BREAK, SLOW, DOT, STUN, FREEZE, SILENCE
- 辅助类：HEAL, ATTACK_BUFF, DEFENSE_BUFF, SPEED_BUFF

### 2.3 攻速机制

| 项目 | 当前状态 | 目标状态 | 状态 |
|------|---------|---------|------|
| 攻速作用 | 无（死字段） | 影响法决CD恢复速度 | ⏳ 待实现 |

**公式：**
```
每回合 CD 减少量 = 1 × attackSpeed
```

**代码修改：**
- [ ] 修改 `DefaultCombatEngine` 的 CD 更新逻辑
- [ ] 玩家获取攻速：从装备的 `EquipmentTemplate.attackSpeed` 读取
- [ ] 怪物/灵兽：默认攻速 1.0

**文档已更新：** `战斗系统.md` §6，`物品装备.md` §2.1

---

## 3. 福地系统

### 3.1 数据结构调整

| 项目 | 当前状态 | 目标状态 | 状态 |
|------|---------|---------|------|
| 地灵数据 | Fudi 表内嵌 | 独立 `xt_spirit` 表 | ✅ 已实现 |

**文档已更新：** `福地.md` §2

### 3.2 天劫机制

| 项目 | 当前状态 | 目标状态 | 状态 |
|------|---------|---------|------|
| 触发方式 | 固定7天 | 玩家可主动发起，7天强制 | ⏳ 待实现 |
| 失败劫数 | 劫数+1 | 劫数不增加 | ⏳ 待实现 |

**代码修改：**
- [ ] 添加主动渡劫命令
- [ ] 修改天劫失败逻辑，劫数不变

**文档已更新：** `福地.md` §6

---

## 4. 灵兽系统

### 4.1 数据结构

| 项目 | 当前状态 | 目标状态 | 状态 |
|------|---------|---------|------|
| 灵兽实体 | 独立 `xt_beast` 表 | 保持 | ✅ 已确认 |
| 兽栏关联 | `penned_cell_id` | 保持 | ✅ 已确认 |

**文档已更新：** `灵兽.md`

---

## 5. 地灵对话系统（重大更新）

### 5.1 AI 驱动情绪系统

| 项目 | 当前状态 | 目标状态 | 状态 |
|------|---------|---------|------|
| 情绪判断 | 硬编码规则 | LLM 通过 Function Tool 自主判断 | ⏳ 待实现 |
| 情绪状态 | 6 种 | 9 种（新增 CURIOUS, BORED, SAD） | ⏳ 待实现 |

**新增情绪状态：**
- CURIOUS（好奇）
- BORED（无聊）
- SAD（伤心）

**代码实现：**
- [ ] 创建 `SpiritEmotionTools` 类
- [ ] 添加 `updateEmotion` 工具方法
- [ ] 添加 `addThought` 工具方法
- [ ] 修改 Prompt 模板，注入情绪上下文

### 5.2 福地事件系统

| 项目 | 当前状态 | 目标状态 | 状态 |
|------|---------|---------|------|
| 事件生成 | 无 | 对话时懒生成，不存库 | ⏳ 待实现 |
| 事件频率 | 无 | 4-8 小时随机 | ⏳ 待实现 |
| 事件消费 | 无 | 每次对话最多 6 条 | ⏳ 待实现 |

**代码实现：**
- [ ] 创建 `FudiEventGenerator` 类
- [ ] 定义 `FudiEvent` 枚举（预定义事件池）
- [ ] 在 `Spirit.chatWithSpirit()` 中集成事件生成
- [ ] Spirit 实体添加 `lastEventTime` 字段

**事件池（预定义）：**
- WEATHER_RAIN, WEATHER_WIND, STRAY_BEAST, SPIRIT_BUTTERFLY
- MYSTERIOUS_VISITOR, CROP_WITHER, BEAST_BIRTH, ENERGY_RESTORE
- MEMORY_OLD_MASTER, MEMORY_FIRST_MEETING, PRANK_SLIME, PRANK_THEFT

### 5.3 对话历史系统

| 项目 | 当前状态 | 目标状态 | 状态 |
|------|---------|---------|------|
| 历史存储 | 无 | 独立 `xt_spirit_history` 表 | ⏳ 待实现 |
| 记忆容量 | 无 | 对数公式增长 | ⏳ 待实现 |

**记忆容量公式：**
```
maxHistory = 5 + floor(5 × log2(tribulationStage + 1))
```

| 劫数 | 记忆上限 |
|------|---------|
| 0 | 5 条 |
| 1 | 10 条 |
| 3 | 15 条 |
| 7 | 20 条 |
| 15 | 25 条 |

**数据库迁移：**
```sql
CREATE TABLE xt_spirit_history (
    id            BIGSERIAL PRIMARY KEY,
    fudi_id       BIGINT NOT NULL,
    role          VARCHAR(16) NOT NULL,
    content       TEXT NOT NULL,
    emotion_state VARCHAR(20),
    create_time   TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_spirit_history_fudi_time ON xt_spirit_history (fudi_id, create_time DESC);
```

**代码实现：**
- [ ] 创建 `SpiritHistory` 实体
- [ ] 创建 `SpiritHistoryMapper`
- [ ] 创建 `SpiritHistoryRepository`
- [ ] 修改 `SpiritChatService` 读取和保存历史

**文档已更新：** `地灵对话.md`（完整重写）

---

## 6. 地图系统

### 6.1 遇怪池格式

| 项目 | 当前状态 | 目标状态 | 状态 |
|------|---------|---------|------|
| 遇怪池格式 | `{"id": weight}` | `{"id": {weight, min, max}}` | ⏳ 待实现 |
| encounter_size | 独立字段 | 合并到 monster_encounters | ⏳ 待实现 |

**数据库迁移：**
```sql
-- 合并 encounter_size 到 monster_encounters
-- 旧格式: {"1": 50, "2": 30}
-- 新格式: {"1": {"weight": 50, "min": 1, "max": 3}, "2": {"weight": 30, "min": 1, "max": 2}}
```

**代码修改：**
- [ ] 修改 `MapNode.monsterEncounters` 类型为 `Map<Long, MonsterSpawn>`
- [ ] 创建 `MonsterSpawn` record
- [ ] 修改怪物生成逻辑

**文档已更新：** `地图旅行历练.md`

---

## 7. 数据库迁移清单

| 迁移文件 | 内容 | 优先级 |
|---------|------|--------|
| V1.x | xt_skill 添加 require_wis, require_skill_id, tags | 中 |
| V1.x | xt_skill 删除 power_multiplier | 中 |
| V1.x | 创建 xt_spirit_history 表 | 高 |
| V1.x | xt_map_node 遇怪池格式迁移 | 中 |

---

## 8. 代码修改清单

### 8.1 用户系统
- [ ] 移除 User 实体中的加点/洗点相关字段和方法
- [ ] 移除 CultivationService 中的加点/洗点方法
- [ ] 移除相关的 Command Handler

### 8.2 法决系统
- [ ] 修改 Skill 实体，添加新字段
- [ ] 修改 DefaultCombatEngine，实现攻速影响CD恢复

### 8.3 福地系统
- [ ] 添加主动渡劫命令
- [ ] 修改天劫失败逻辑

### 8.4 地灵对话系统
- [ ] 创建 SpiritEmotionTools
- [ ] 创建 FudiEventGenerator
- [ ] 创建 SpiritHistory 相关类
- [ ] 修改 SpiritChatService 集成新功能
- [ ] 修改 Prompt 模板

### 8.5 地图系统
- [ ] 创建 MonsterSpawn record
- [ ] 修改 MapNode 实体
- [ ] 修改怪物生成逻辑

---

## 9. 文档更新清单

| 文档 | 状态 |
|------|------|
| 用户.md | ✅ 已更新 |
| 法决.md | ✅ 已更新 |
| 战斗系统.md | ✅ 已更新 |
| 物品装备.md | ✅ 已更新 |
| 福地.md | ✅ 已更新 |
| 灵兽.md | ✅ 已更新 |
| 地灵对话.md | ✅ 已更新 |
| 地图旅行历练.md | ✅ 已更新 |
| 命令参考.md | ⏳ 待更新（添加新命令） |
| 系统架构与规范.md | ⏳ 待更新（服务清单） |
