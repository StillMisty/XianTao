# 更新路线图 — 仙道代码质量审计与重构计划

## 一、P0 — 关键问题（立即修复，阻塞上线） ✅ 已全部修复

### 1.3 `PlayerCombatant` 注入 Repository — 领域层/基础设施层边界破坏 ✅

**文件**: `domain/monster/PlayerCombatant.java`
**修复**: 移除构造函数中的 Repository 依赖，改为接收预解析的 `Equipment weapon` 和 `double attackSpeed`。武器和攻速的查询移至 `CombatService.buildPlayerTeam`。

---

## 二、P1 — 高风险问题（本迭代修复） ✅ 已全部修复

### 2.2 `UserBounty.status` 使用原始 String 而非枚举 ✅

**修复**: 创建 `BountyStatus` 枚举（`ACTIVE`, `COMPLETED`, `ABANDONED`），替换 `UserBounty.status` 的 `String` 类型。新增 Flyway 迁移 `V1.0.31` 更新数据库约束和现有数据。

### 2.3 枚举 `fromCode()` 三种不同错误处理策略 ✅

**修复**: 统一所有 18 个枚举的 `fromCode()` 方法为抛出 `IllegalArgumentException`。更新 3 个调用点处理异常。

### 2.4 Repository 三种不同的 save 策略 ✅

**修复**: 统一所有 12 个仓库实现使用 `insertOrUpdateSelective` 策略。

```java
@Column(onInsertValue = "'active'")
private String status;  // "active" / "completed" 是魔法字符串
```

`isActive()` 和 `isCompleted()` 直接硬编码字符串比较。应定义为 `enum BountyStatus { ACTIVE, COMPLETED }`。

### 2.3 枚举 `fromCode()` 三种不同错误处理策略

| 行为 | 涉及的枚举 |
|------|-----------|
| **返回 `null`** | `PlatformType`, `UserStatus`, `AttributeType`, `ItemType`, `EquipmentSlot`, `WeaponType`, `AffixType`, `MonsterType`, `ElementType`, `TravelEventType` |
| **抛出 `IllegalArgumentException`** | `CellType`, `MBTIPersonality`, `MutationTrait`, `EmotionState`, `BeastQuality` |
| **返回默认值** | `Rarity`(COMMON), `SkillType`(ACTIVE), `PillQuality`(NORMAL), `EffectType`(DAMAGE), `BindingType`(NONE) |

调用者必须根据不同的枚举使用不同的错误处理方式。统一为一种策略。

### 2.4 Repository 三种不同的 save 策略

| 模式 | 使用的 Repo | 问题 |
|------|-----------|------|
| `insertOrUpdateSelective` | User, FudiCell, Fudi, Spirit | 仅更新非 null 字段，不会覆盖已设置值 |
| `insertOrUpdate`（非 selective） | StackableItem, Equipment | 覆盖所有列为 null |
| 手动 null-check → `insert`/`update` | Beast, PlayerSkill, MonsterTemplate | 非原子操作 |

三种策略混用会导致不同的行为 Bug。统一为一种。

## 三、P2 — 中优先问题（下个迭代修复） ✅ 已全部修复

### 3.1 代码重复（5 处跨服务重复） ✅

- `calculateSingleProtectorBonus` + `isInSameLocation` → 提取至 `ProtectionHelper` 组件
- `getCropTier` → `ItemResolver` 委托给 `FudiHelper`
- Skill 名称匹配 → `SkillService` 提取通用 `resolveByIndexOrName` 辅助方法
- 装备属性变更计算 → 统一使用 `calculateAttributeChange` 方法

### 3.2 竞态条件 — Check-Then-Act 模式 ✅

**修复**: 在 `UserRepository` 添加原子 `deductSpiritStonesIfEnough(userId, cost)` 方法，使用 `WHERE spirit_stones >= #{cost}` 原子更新。移除 6 个调用点的 `checkSpiritStones` + `deductSpiritStones` 两步操作。

### 3.3 13 个超长方法（ >50 行） ✅

**修复**: 从 13 个方法中提取了共 40+ 个私有辅助方法，将所有方法缩减至 50 行以下。

### 3.4 7 个超大文件（ >400 行） ✅

**修复**: 提取新服务类:
- `FudiService` → `FudiGiftService` + `FudiCollectService`
- `TrainingService` → `TrainingCombatLogic`
- `BountyService` → `BountyCombatService`
- `CultivationService` → `DaoProtectionService`
- `BeastBreedingService` → `BeastEvolutionService` + `BeastMutationService`
- `PillRefiningService` → `PillCombinationFinder`

### 3.5 VO 可变性不一致 ✅

**修复**: 所有 8 个 `@Data @Builder` VO（`BreakthroughResult`, `DaoProtectionResult`, `InventoryResult`, `EquipResult`, `CharacterStatusResult`, `AttributeChange`, `UserStatusVO`, `InventoryItem`）转换为不可变 `record`，并移除 `@Builder` 改用直接构造函数调用。

### 3.6 `Combatant.heal()` 静默空实现 ✅

**修复**: `heal()` 从 default 方法改为 abstract 方法。验证所有 4 个实现类均已覆盖。

### 3.7 缺少 `@Transactional` 读写不一致 ✅

**修复**: 在以下方法添加 `@Transactional`:
- `FudiService.getFudiStatus(Long)`（内部方法写入 Spirit + Fudi 状态）
- `BeastDisplayHelper.clearBeastCell`（保存 fudiCellRepository）
- `FudiHelper.getFudiByUserId`（写入 Fudi 在线时间和 Spirit 情绪状态）

### 3.9 `DamageCalculator.evaluateExpression` 公式求值脆弱 ✅

**修复**: 将脆弱的分割式表达式求值器替换为支持 `+`, `-`, `*`, `/` 和括号的递归下降解析器，具有正确的运算符优先级。

### 3.10 Entity 的 `staticConstructor` 与 ORM 冲突风险 ✅

**修复**: 从 6 个实体（`User`, `DaoProtection`, `Fudi`, `Spirit`, `SpiritForm`, `MapNode`）移除 `@Data(staticConstructor = "create")`，替换为 `@Data @NoArgsConstructor` 和显式的 `create()` 工厂方法。

### 3.11 `UserBountyRepository` 暴露持久化细节 ✅

**修复**: 将单独的 `save()`/`update()` 方法合并为单一的 `save()` 方法，使用 `insertOrUpdateSelective`。

### 3.12 领域实体贫血化 ✅

**修复**: 为 11 个贫血实体添加领域行为方法：
- `DaoProtection`: `isProtegeOf`, `isProtectorOf`, `create`
- `Bounty`: `requiresLevel`, `hasRewards`, `getTotalEventWeight`
- `SpiritForm`: `likes`, `dislikes`
- `SpiritHistory`: `isFromUser`, `isFromSpirit`
- `MonsterTemplate`: `hasSkills`, `hasDropTable`, `getDropWeightTotal`
- `Skill`: `isActive`, `isPassive`, `requiresPrecursor`, `meetsWisRequirement`, `meetsLevelRequirement`
- `PlayerSkill`: `isEquipped`, `equip`, `unequip`, `create`
- `UserAuth`: `matches`
- `PlayerBuff`: `isExpired`, `isActive`
- `PlayerPillRecipe`: `isRecipeFor`
- `PillResistance`: `incrementCount`, `isResistant`, `getResistanceLevel`

---

## 四、P3 — 低优先问题（技术债务，择机处理）

### 4.1 构建工具链缺失

- **JaCoCo** 未配置 — 无法衡量覆盖率
- **OWASP dependency-check** 未配置 — 无漏洞扫描
- **Error Prone / NullAway** 未配置 — 无静态分析
- **Gradle wrapper validation** 未配置
- 测试无并行执行（`maxParallelForks`）

### 4.2 CELL CONFIG 类型处理器静默吞错误

**文件**: `infrastructure/mybatis/handler/CellConfigTypeHandler.java:46-51`

```java
try {
    return CellType.valueOf(code.toUpperCase());
} catch (SQLException e) {
    return null;  // 静默错误
}
```

当 `cellType` 为 null 时默认返回 `EmptyConfig.class`——DB 读取错误被完全掩盖。

### 4.3 MyBatis JSONB TypeHandler 无 ObjectMapper 配置

**文件**: `JsonbTypeHandler`, `JsonbCollectionTypeHandler`, `CellConfigTypeHandler`, `BountyRewardListTypeHandler`

全部创建 `new ObjectMapper()` 默认配置，无 `JavaTimeModule` 注册。如果任何 JSONB 字段包含 `LocalDateTime` 或 `Instant`，反序列化将失败。

### 4.4 `HelpCommandHandler` 未实现 `CommandGroup`

**文件**: `handle/command/HelpCommandHandler.java`

所有 9 个 CommandHandler 都实现 `CommandGroup` 用于自动发现，唯独 HelpCommandHandler 不实现——命令文档中不会自描述。

### 4.5 LeaderboardCommandHandler 缺少错误前缀

**文件**: `handle/command/LeaderboardCommandHandler.java:21,28`

所有其他 CommandHandler 的错误响应都以 `❌` 开头，Leaderboard 的却直接返回裸消息。

### 4.7 SpiritPromptTemplates 文本块缩进注入 LLM 提示词

**文件**: `service/ai/SpiritPromptTemplates.java:36-66`

Java 文本块的缩进空白被原样发送给 LLM——浪费 Token 且可能混淆模型。应使用 `.stripIndent()`。

### 4.8 `@Data` Entity 的 equals/hashCode 行为不一致

所有未扩展 `Model<T>` 的 Entity（`Beast`, `Equipment`, `StackableItem`, `FudiCell` 等）的 `equals`/`hashCode` 基于所有字段。实体 null `id` 时基于值，有 `id` 后基于标识——经典的 ORM 反模式。

### 4.9 SQL 编写一致性

- V1.0.17: 内联 `REFERENCES` 语法，与其他迁移的命名约束不一致
- V1.0.5: 武器类型 CHECK 使用中文值 `('刀兵', '长兵', '远兵', '奇兵')` — JDBC 兼容性风险

### 4.11 `FudiHelper.getFudiByUserId` 方法名误导

"Get" 方法内部修改了 `Fudi.lastOnlineTime` 和 `Spirit.emotionState` 并保存——有写入副作用的方法不应叫 `get`。

### 4.12 测试中 `assertNotNull(result)` 但从不验证结果

- `PillRefiningServiceTest.refinePillAuto_withPerfectMatch` 仅检查非 null
- 无 AI 服务测试
- 无 ItemUseHandler 策略测试
- 无 CommandHandler 格式化测试

---

## 五、架构改进建议（非阻塞，长期规划）

### 5.1 引入应用层 Application Service 门面

当前 Handle 层直接调用多个 Domain Service，缺乏用例编排。引入 Application Service 可：
- 将跨多个 Service 的复杂操作（如战斗后领取奖励+经验+掉落）封装为单一用例
- 减少 Handle 层的业务逻辑泄漏
- 提供清晰的边界用于事务管理

### 5.2 统一 `@Transactional` 策略

目前 `@Transactional` 标注位置不一致（Service 方法 vs Repository 方法）。应：
- 仅在 Application Service 层标注 `@Transactional`
- Domain Service 内部方法不管理事务
- Repository 方法不管理事务（由上层控制）

### 5.3 提取共享内核

`calculateSingleProtectorBonus`, `isInSameLocation`, `getCropTier` 等被多处使用的逻辑应提取至 `top.stillmisty.xiantao.domain.shared` 包。

### 5.4 ItemProperties 深度嵌套重构

`ItemProperties.java`(75 行) 中 `BeastEgg.BeastSkillPool.InnateSkill` 是 3 层嵌套。考虑将每个 Property 类型提取至独立文件，使用包内可见性控制访问。

### 5.5 Spring AI 集成测试

Spring AI 2 的 ChatClient 需要网络连接才能测试。应使用 WireMock 或 Spring AI 的测试支持来验证 Function Calling + 提示词模板。
